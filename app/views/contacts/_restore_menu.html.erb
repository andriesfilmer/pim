<div class="row">
  <div class="columns medium-6" id="add-back-arrow cursor-pointer"></div>
  <div class="columns medium-6 text-right" id="add-back-arrow">
    <p><%= button_to "Restore version #{@contact.id}", contactrestore_path,
      params: { version_id: @contact.id} , class: "button" if @contact.id %></p>
  </div>
</div>
