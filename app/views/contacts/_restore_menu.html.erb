<div class="row">
  <div class="columns text-right" id="add-back-arrow">
    <img src="/assets/restore.svg" class="icon float-left">
    <%= content_tag :span, '', class: "divider" if @contact.id %>
    Version <%= @contact.id %>
    <%= button_to "Click to restore", contactrestore_path, data: { turbo: false },
      params: { version_id: @contact.id} , class: "button" if @contact.id %>
  </div>
</div>
