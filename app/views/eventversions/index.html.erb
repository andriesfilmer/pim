<div data-controller="event">
  <div class="row">
    <div class="columns" id="add-back-arrow">
      <img src="/assets/restore.svg" class="icon large float-left">
      <h2>Versions</h2>
    </div>
  </div>

  <div class="row row-th hide-for-small-only">
    <div class="medium-8 columns">Created</div>
    <div class="medium-4 columns text-right">Select version</div>
  </div>
  <% @eventversions.each do |eventversion| %>
  <div id="versions" class="row odd-even">
    <div class="small-8 medium-8 columns">
      <%= link_to (l eventversion.created_at, format: :long), eventversion_path(eventversion) %>
    </div>
    <div class="small-4 medium-4 columns text-right">
      <img src="assets/difference.svg" class="icon large">
      <%= check_box_tag "version_ids[]", eventversion.id, false, id: eventversion.id,
        data: { action: "click->event#versions" }, class: "checkBoxClass" %>
      <%= content_tag :span, eventversion.id %>
    </div>
  </div>
  <% end %>

  <div class="row">
    <div class="medium-10 columns"></div>
    <div class="medium-2 columns text-right">
      &nbsp;

      <%= button_to "Compare versions", eventversions_compare_path, id: "compare_btn", class: "button disabled", disabled: true,
        params: { ids: "" }, data: { turbo: false }, method: :get %>

    </div>
  </div>

</div>
